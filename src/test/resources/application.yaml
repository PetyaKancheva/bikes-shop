spring:
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: never

#logging:
#  org:
#    hibernate:
#      descriptor: TRACE
#      SQL: WARN
#    nodeValue: WARN
#    blog: WARN

logging:
  level:
    root: WARN
    org:
      springframework:
        web: WARN
        jdbc.core: INFO
      hibernate:
        type: INFO
        SQL: INFO



frankfurter:
  schema: https
  host: dummy
  path: /v1/latest
  symbols: BGN,USD,PLN
  enabled: true


bikes:
  rememberMeKey: key


mail:
  email: "test@mail.com"
  host: localhost
  port: 1111
  username: email@mail.com
  password: password

comments-server:
  schema: http
  host: localhost
  port: 2222
  path: /
  enabled: true